apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-cm1
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://prometheus-service
        isDefault: true